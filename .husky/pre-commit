#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

npm run build
git add -A
git reset -- assets/manifest.json
git reset -- snippets/vite-tag.liquid
git update-index --no-skip-worktree snippets/vite-tag.liquid
git update-index --no-skip-worktree assets/manifest.json
npx lint-staged
